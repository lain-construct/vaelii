# check-doc-drift.py allowlist — one literal token per line, matched against the
# flagged token. `#` comments allowed. A dead entry (matched nothing this run)
# is reported as an advisory — prune it.

# Third-party vars legitimately named in docs (taoensso.trove, hiccup2.core).
trove/log!
h/raw

# Unit, not a namespaced var (`m/s` in docs/quantity.md).
m/s

# The Maven layout, named in CONTRIBUTING.md and .claude/rules/workflow.md as
# the one this repo does NOT use — a path that exists nowhere on purpose.
src/main/clojure

# A lexeme's namespace is the KB's, not a Clojure alias — `lex/fool's_gold` names a
# surface form in a sentence (docs/naming.md), so there is no var to resolve it to.
lex/fool's_gold
